pipeline {
    agent any 

    stages {
        stage('Build Assets') {
            agent any 
            steps {
                echo 'Building vvvvvAssets...'
                sh 'sudo ./install.sh'
           }
        }
        
        
        
    }
}
